{
  "name": "vimigo-tcvr-os",
  "private": true,
  "version": "0.1.0",
  "description": "vimigo TCVR Revenue OS Calculator — Traffic × Conversion × Value × Recurring diagnostic system",
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\"",
    "dev:web": "pnpm --filter frontend dev",
    "dev:api": "pnpm --filter backend dev",
    "build": "pnpm --filter frontend build",
    "start": "pnpm --filter backend start",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "packageManager": "pnpm@11.5.0"
}
