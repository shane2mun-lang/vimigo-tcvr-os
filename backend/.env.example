# Backend reads from the repo-root .env (see ../.env.example). This file documents
# the same variables for anyone running the backend standalone.
ANTHROPIC_API_KEY=
PORT=3001
MODEL_SMART=claude-opus-4-8
MODEL_FAST=claude-sonnet-4-6
SCAN_FETCH_TIMEOUT_MS=8000
